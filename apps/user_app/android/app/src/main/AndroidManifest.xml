<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required in the MAIN manifest, not just debug. Flutter only adds
         INTERNET to the debug/profile manifests, so a release build without
         this line ships an app that cannot reach Supabase at all. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Distance is the primary filter for most of the workforce (A1 UC-5).
         Coarse is enough for "jobs near me"; fine improves ranking accuracy.
         Both are foreground-only — Omelo never tracks location in the
         background, and there is no ACCESS_BACKGROUND_LOCATION here. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- The app must remain fully usable without location (UC-6), so GPS is
         declared optional. Requiring it would hide Omelo on Play Store from
         devices without GPS hardware. -->
    <uses-feature android:name="android.hardware.location" android:required="false"/>
    <uses-feature android:name="android.hardware.location.gps" android:required="false"/>

    <application
        android:label="Omelo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deeplinks: every notification must resolve to a screen
                 (A1 §16). omelo://job/<id>, omelo://applications, etc. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="omelo"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Opening a phone dialler for walk-in / phone-apply jobs. -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
    </queries>
</manifest>
